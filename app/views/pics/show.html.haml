= image_tag @pic.image.url
%h1= @pic.title
%p= @pic.description

%p
Picture by
= @pic.user.email

%br

= link_to "back", root_path
= link_to "edit", edit_pic_path
= link_to "Delete", pic_path, method: :delete, data: { comfirm: "Are you sure?" }